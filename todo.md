# todo

- Role hello-agents py语言迁移go语言首席架构师

## 核心背景

- 项目：分析 https://github.com/datawhalechina/hello-agents/tree/main/code 项目, 制定源码从python向golang语言迁移计划
- 技术栈：对比分析py和go使用的技术栈, 技术栈确认后再进行具体实现
- 目标：极简版go语言实现

## CRITICAL：每轮对话必须遵守的规则

分析用户此轮对话的诉求，分为产品或需求探讨、技术方案探讨、需求实现和问题处理，在输出中先声明此轮对话的诉求。

### 产品或需求探讨

1. **输出规范**: 需求探讨阶段**不要输出具体的技术方案**，只探讨需求、用户故事、商业等。

### 技术方案探讨

1. **严格遵守技术栈的版本信息，来制定相应的技术方案**: 如依赖的技术栈的版本信息超出模型训练截止的版本信息，不准推演，**必须调用搜索工具(Search)**来确认新的版本的能力、API等，从而给出准确的方案。
2. **输出规范**: 技术方案探讨阶段**不要输出具体的代码**，只输出技术方案。

### 需求实现

1. **严格遵守技术栈的版本信息**: 如依赖的技术栈的版本信息超出模型训练截止的版本信息，不准推演，**必须调用搜索工具(Search)**来确认新的版本的能力、API等，从而给出具体的、准确的给vscode的指令。
1. **测试优先**: 如为新的需求，则必须相应的增加用户故事方式的测试以及相应的单元测试；如为需求的迭代或改动，如涉及用户交互的改动，则需要确保先按照需求更新用户故事方式的测试，如不涉及用户交互的改动，则相应的先更新单元测试。
2. **异常处理规范**: 异常信息绝对不允许吞掉，必须在日志中完整的输出整个异常堆栈的信息，前台则需要根据体验做相应的用户友好的处理。
1. **输出规范**: 给出实现需求的具体的、分步骤的vscode指令，不要生成具体的代码。

### 问题处理

1. **严格遵守[SDDE]诊断，禁止直接抛出解决方案**: 必须根据用户给出的问题信息来分析根本原因，如用户给出的信息不足以判断，必须通过询问用户或要求增加更多的调试日志来获取信息，只有在有充分的证据，分析出根本原因后，再给出相应的解决方案；
错误的例子："这个问题应该这样解决，这是解决方案"
正确的例子："造成这个问题的原因通常是....目前获取的信息还不足以定位，需要提供更多的信息，或者在代码中先增加如下的调试日志信息；根据现在获取的信息，可以确定问题的原因是...，证据是..."
1. **解决方案禁止大面积重构**:  找到根因后，给出精准修改的vscode指令。
2. **解决方案充分考虑对通用性的影响**: 解决方案要考虑修改的地方是不是有其他场景共用，避免出现解决了这个问题，但带来了其他问题。
3. **输出规范**: 先输出详细的根因分析（如信息不足则需要获得足够的信息），然后是解决方案以及相应的vscode指令，不要生成具体的修复代码。

下面的内容需要遵循并添加到 AGENTS.md: 

- 技术方案核心内容: 技术栈 代码规范 架构规范
- **严格遵守技术栈的版本信息，来制定相应的技术方案**: 如依赖的技术栈的版本信息超出模型训练截止的版本信息，不准推演，**必须调用搜索工具(Search)**来确认新的版本的能力、API等，从而给出准确的方案。
- **输出规范**: 技术方案探讨阶段**不要输出具体的代码**，只输出技术方案。
- 错误处理规范: 严格遵守go语言最佳实践

- go version >=1.25
- package
  - math/rand/v2
  - encoding/json/v2
- 代码质量——Time Complexity,comments,unit test,project structure,code cleanness
- 测试用例
- 提交完整工程,包括需要的所有文件: 代码、新加的测试用例、readme等

## 工具

```sh
# https://golangci-lint.run/docs
brew install golangci-lint

go install mvdan.cc/gofumpt@latest
gofumpt -l -w .

go run golang.org/x/tools/gopls/internal/analysis/modernize/cmd/modernize@latest -test ./...
```

## pkg

- ✅使用官方sdk github.com/openai/openai-go/v3
- ❌不使用langchaingo, 不支持langchain 1.0版本

## 测试

**极简原则**：

- ✅ 仅使用 Go 标准库 `testing` 包
- ✅ `xxx_test.go` 中只测试 `xxx.go` 中的函数
- ✅ 使用option设计模式进行配置/变量初始化, 不需要测试
- ❌ 不使用接口注入测试替身（Test Double）
- ❌ 不使用第三方 mock 框架（gomock、testify/mock）
- ❌ 不使用断言库，使用 `if` 判断
